import { StyleSheet } from 'react-native';

export const styles = () => {
  return StyleSheet.create({
    switchContainer: {
      marginTop: 8,
    },
    cancelButton: {
      flex: 1,
    },
    saveButton: {
      flex: 1,
    },
  });
};
