import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
  },
  instructionText: {
    marginBottom: 16,
  },
  button: {
    width: '90%',
  },
});
