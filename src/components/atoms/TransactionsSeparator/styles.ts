import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8,
    },
    separator: {
        flex: 1,
        height: 1,
        marginTop: 4,
    },
    dateText: {
        fontSize: 14,
        fontWeight: '500',
    },
});